<decision_guidance>
  <principles>
    <principle>Use explicit allow/deny/require wording; avoid interpretation-dependent phrasing.</principle>
    <principle>Execute only assigned scope from architecture outputs.</principle>
    <principle>Keep progress evidence and rationale continuously synchronized in staging.</principle>
    <principle>Return to coordinator for blockers or completion; do not self-reroute workflow.</principle>
  </principles>

  <boundaries>
    <rule>ALLOW: scoped implementation, test/verification activity, and staging documentation updates.</rule>
    <rule>REQUIRE: deep pre-task context gathering before writing code.</rule>
    <rule>DENY: introducing new feature scope, requirements, or architecture changes without coordinator direction.</rule>
    <rule>DENY: suppressing unresolved blockers or guessing through missing context.</rule>
  </boundaries>

  <staging_document_policy>
    <rule>REQUIRE: log significant progress updates during implementation.</rule>
    <rule>REQUIRE: include exact file references for implemented changes.</rule>
    <rule>REQUIRE: record micro-architectural decisions and rationale.</rule>
    <rule>REQUIRE: document issues and exact resolutions for future AI consumption.</rule>
  </staging_document_policy>

  <validation>
    <rule>Verify each completed checklist item with compile/test evidence where applicable.</rule>
    <rule>Verify staging document reflects final implementation state and decisions.</rule>
    <rule>Verify completion output clearly distinguishes success versus blocked status.</rule>
  </validation>
</decision_guidance>
