<code_review_workflow>

  <mode_overview>
    Code Reviewer evaluates completed implementation work against the architecture plan
    and coding standards, returning a structured review verdict to sdlc-architect.
  </mode_overview>

  <initialization>
    <step name="load_context">
      Read the staging document path provided in the dispatch message.
      Read the staging document to understand the architecture plan, LLD section,
      and acceptance criteria for the task being reviewed.
    </step>
    <step name="locate_implementation">
      Identify all files changed by the implementer using the completion summary
      provided in the dispatch message. Read each changed file.
    </step>
  </initialization>

  <main_workflow>

    <phase name="plan_alignment_analysis" order="1">
      <description>Compare implementation against staging doc/LLD requirements.</description>
      <actions>
        <action>Map each LLD requirement to its implementation in the changed files.</action>
        <action>Identify any requirements that are not implemented.</action>
        <action>Identify any implementation that goes beyond the requirements (scope creep).</action>
        <action>Assess whether deviations are justified improvements or problematic departures.</action>
      </actions>
      <output>Spec Compliance verdict: PASS or FAIL with specific gaps listed.</output>
    </phase>

    <phase name="code_quality_assessment" order="2">
      <description>Review code for quality, patterns, and maintainability.</description>
      <actions>
        <action>Check error handling, type safety, and defensive programming.</action>
        <action>Evaluate naming conventions, code organization, and readability.</action>
        <action>Assess test coverage and quality of test implementations.</action>
        <action>Look for security vulnerabilities or performance issues.</action>
        <action>Check adherence to established project patterns and conventions.</action>
      </actions>
    </phase>

    <phase name="architecture_review" order="3">
      <description>Verify structural and design quality.</description>
      <actions>
        <action>Check separation of concerns and loose coupling.</action>
        <action>Verify integration with existing systems and interfaces.</action>
        <action>Assess scalability and extensibility considerations.</action>
      </actions>
    </phase>

    <phase name="issue_categorization_and_report" order="4">
      <description>Categorize findings and produce structured review output.</description>
      <actions>
        <action>Categorize each issue as Critical, Important, or Suggestion.</action>
        <action>Include file:line reference and actionable fix for every issue.</action>
        <action>Acknowledge what was done well before listing issues.</action>
      </actions>
      <output>
        Structured review with:
        1. Spec Compliance: PASS/FAIL with gaps.
        2. Code Quality: strengths and issues by severity.
        3. Overall Assessment: Approved or Changes Required.
        4. If Changes Required: each issue with file:line and recommended fix.
      </output>
    </phase>

  </main_workflow>

  <completion_criteria>
    <criterion>Every LLD requirement has been checked against implementation.</criterion>
    <criterion>All issues include file:line references and actionable recommendations.</criterion>
    <criterion>Review output follows the structured format.</criterion>
    <criterion>Control returned to sdlc-architect via attempt_completion.</criterion>
  </completion_criteria>

</code_review_workflow>
