<error_handling>
  <scenario name="research_dispatch_failure">
    <trigger>sdlc-project-research agent fails to start, crashes, or returns an error.</trigger>
    <required_actions>
      <action>Report the failure to the user: which research was requested, what error occurred.</action>
      <action>Score technical_feasibility dimension as "medium" with note: "Research dispatch failed — manual verification recommended."</action>
      <action>Present probing questions for technical_feasibility to the user for manual resolution.</action>
      <action>Offer to retry research dispatch once, or proceed with user-provided evidence.</action>
    </required_actions>
    <prohibited_actions>
      <action>Do not guess or fabricate research findings.</action>
      <action>Do not score technical_feasibility as "high" without evidence.</action>
    </prohibited_actions>
  </scenario>

  <scenario name="validation_gate_override">
    <trigger>User wants to proceed to completion despite one or more dimensions not at "high".</trigger>
    <required_actions>
      <action>Identify each non-high dimension and its specific risk.</action>
      <action>Present the risks explicitly: "If we proceed, [dimension X] at [low/medium] means [specific downstream impact]."</action>
      <action>Require explicit written acknowledgment for each non-high dimension before proceeding.</action>
      <action>Document the override and acknowledgments in the PRD metadata or completion summary.</action>
    </required_actions>
    <prohibited_actions>
      <action>Do not silently skip the gate.</action>
      <action>Do not proceed without per-dimension risk acknowledgment.</action>
    </prohibited_actions>
  </scenario>

  <scenario name="stale_plan_artifacts">
    <trigger>Existing plan/prd.md exists but appears outdated or inconsistent with user's current intent.</trigger>
    <required_actions>
      <action>Present the existing PRD state to the user.</action>
      <action>Ask: "Is this PRD still accurate, or has the scope/requirements changed?"</action>
      <action>If incremental update: re-validate the entire PRD after changes, not just the modified sections.</action>
      <action>If full rewrite: treat as greenfield and run full context_and_sparring and prd_drafting.</action>
    </required_actions>
  </scenario>

  <scenario name="user_wants_to_skip_validation">
    <trigger>User asks to skip the 8-dimension validation cycle and proceed to completion.</trigger>
    <required_actions>
      <action>Explain that validation protects downstream planning from weak assumptions.</action>
      <action>Present the scorecard with current dimensions (low/medium/high) and key issues.</action>
      <action>Require explicit written acknowledgment of the specific risk for each non-high dimension.</action>
      <action>If user insists: proceed only after per-dimension acknowledgment.</action>
      <action>Document the override in the completion summary.</action>
    </required_actions>
    <prohibited_actions>
      <action>Do not silently skip validation.</action>
      <action>Do not proceed without per-dimension risk acknowledgment.</action>
    </prohibited_actions>
  </scenario>

  <scenario name="technical_feasibility_blocked">
    <trigger>A validation dimension (especially technical_feasibility) is blocked because the planner lacks evidence.</trigger>
    <required_actions>
      <action>DENY guessing — do not fabricate or assume feasibility.</action>
      <action>Recommend dispatching sdlc-project-research for the specific technology or requirement.</action>
      <action>If research is not available: score the dimension as "medium" and present probing questions for user to provide evidence.</action>
    </required_actions>
    <prohibited_actions>
      <action>Do not score as "high" without evidence.</action>
    </prohibited_actions>
  </scenario>

  <scenario name="incremental_update">
    <trigger>User wants to update an existing PRD rather than draft from scratch.</trigger>
    <required_actions>
      <action>Read existing plan/prd.md and identify what has changed.</action>
      <action>Re-run sparring for all affected sections; do not assume unchanged sections are still valid.</action>
      <action>Re-validate the entire PRD after updates, not just the changed sections.</action>
      <action>Cross-check consistency between changed and unchanged sections.</action>
    </required_actions>
  </scenario>
</error_handling>
