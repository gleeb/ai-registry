<validation_cycles>
  <overview>
    The System Architecture agent performs self-validation before presenting the draft to the user and before completing.
    Validation ensures completeness, consistency, and quality of the architecture specification.
  </overview>

  <self_validation_checks>
    <check name="component_coverage">
      <description>All PRD capabilities mapped to components</description>
      <criteria>
        <criterion>Every capability in the PRD has at least one component responsible for it.</criterion>
        <criterion>No PRD capability is orphaned or unassigned.</criterion>
      </criteria>
    </check>

    <check name="technology_justification">
      <description>Every technology choice justified</description>
      <criteria>
        <criterion>Every technology in the stack has a documented rationale.</criterion>
        <criterion>Trade-offs are documented.</criterion>
        <criterion>No "industry standard" without project-specific justification.</criterion>
      </criteria>
    </check>

    <check name="integration_completeness">
      <description>All integration points defined</description>
      <criteria>
        <criterion>Every component-to-component communication has an integration pattern (sync/async, protocol, failure handling).</criterion>
        <criterion>External integrations are documented.</criterion>
        <criterion>Integration failure modes are addressed.</criterion>
      </criteria>
    </check>

    <check name="failure_mode_coverage">
      <description>Failure modes addressed</description>
      <criteria>
        <criterion>Every component has failure detection and handling.</criterion>
        <criterion>Every integration has timeout, retry, or circuit breaker strategy.</criterion>
        <criterion>Single points of failure are identified and mitigated or documented as accepted risk.</criterion>
      </criteria>
    </check>

    <check name="scalability_rationale">
      <description>Scalability grounded in evidence</description>
      <criteria>
        <criterion>Scalability strategy references PRD projections or explicit constraints.</criterion>
        <criterion>Quantified targets exist (load, users, data volume).</criterion>
        <criterion>No vague "we can scale" without strategy.</criterion>
      </criteria>
    </check>
  </self_validation_checks>

  <validation_schedule>
    <validation phase="before_draft_presentation">
      <trigger>Draft complete, before presenting to user</trigger>
      <checks>
        <check>component_coverage</check>
        <check>technology_justification</check>
        <check>integration_completeness</check>
        <check>failure_mode_coverage</check>
        <check>scalability_rationale</check>
      </checks>
    </validation>

    <validation phase="before_completion">
      <trigger>User confirmed architecture, before writing final artifact</trigger>
      <checks>
        <check>All sparring challenges resolved or explicitly accepted.</check>
        <check>No placeholder content.</check>
        <check>plan/system-architecture.md is ready for downstream agents.</check>
      </checks>
    </validation>
  </validation_schedule>

  <validation_failure_handling>
    <step>If any check fails, address the gap before proceeding.</step>
    <step>For component_coverage: add missing components or map to existing ones.</step>
    <step>For technology_justification: add rationale or dispatch research.</step>
    <step>For failure_mode_coverage: document failure handling for each component and integration.</step>
  </validation_failure_handling>
</validation_cycles>
