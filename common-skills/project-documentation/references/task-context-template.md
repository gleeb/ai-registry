# Task Context Document Template

Use this template when the engineering hub creates per-task context documents during Phase 1b. Each task gets its own context document at `docs/staging/US-NNN-name.task-N.context.md`.

**Purpose:** Eliminate redundant plan-artifact reads across subagent dispatches. Subagents read this document instead of chasing references through story.md, hld.md, api.md, security.md, and testing-strategy.md.

**Who writes it:** The engineering hub during Phase 1b (one-time extraction) and before each Phase 2 dispatch (source file updates).

**Who reads it:** Implementer, Code Reviewer (for plan context + source files). QA reads plan context sections only — QA always reads source files fresh from disk.

**Who must NOT edit it:** Any subagent. This is a hub-managed artifact updated only by the engineering hub between dispatches.

---

## Template

```markdown
# Task N Context: [Task Name]

**Story:** US-NNN — [Story Title]
**Task:** T-N of M
**Generated by:** Engineering Hub (Phase 1b)
**Last updated:** [timestamp — hub updates before each dispatch]

> DO NOT EDIT THIS FILE. It is hub-managed. Subagents read it; the hub writes it.

---

## Acceptance Criteria

> Source: `plan/user-stories/US-NNN-name/story.md` lines X–Y

[Verbatim copy of the acceptance criteria relevant to this task from story.md.
Copy exact text — no paraphrasing, no summarization.]

---

## Design Specification

> Source: `plan/user-stories/US-NNN-name/hld.md` lines X–Y ([DU/IU name])

[Verbatim copy of the relevant design unit and implementation unit sections from hld.md.
Include function signatures, interface definitions, integration points, and boundaries.]

---

## API Contract

> Source: `plan/user-stories/US-NNN-name/api.md` lines X–Y
> [Omit this section if the task has no API surface.]

[Verbatim copy of the relevant API contract sections — endpoint definitions, request/response
shapes, validation rules, error codes.]

---

## Security Controls

> Source: `plan/user-stories/US-NNN-name/security.md` lines X–Y
> [Omit this section if no security controls apply to this task.]

[Verbatim copy of security requirements, threat model notes, and control specifications
relevant to this task's scope.]

---

## Design References

> Source: `plan/user-stories/US-NNN-name/design/`
> [Omit this section if the story has no design artifacts or this task has no UI component.]

[List paths to mockups, wireframes, component specs, or Figma links relevant to this task.
For each, include a brief description of what it shows and what decisions it informs.]

- `plan/user-stories/US-NNN-name/design/[filename].[ext]` — [what it shows, e.g., "Dashboard layout with sidebar nav and card grid"]
- `plan/user-stories/US-NNN-name/design/component-specs.md` lines X–Y — [what section, e.g., "Button variants: primary, secondary, disabled states"]
- Figma: [URL] — [what the prototype covers, e.g., "Interactive settings flow with modal confirmations"]

---

## Testing Requirements

> Source: `plan/cross-cutting/testing-strategy.md`

**AC traceability:**
[Verbatim from the traceability table — which test types cover each AC for this task.]
| AC | Test Type | Notes |
|----|-----------|-------|

**Coverage thresholds (this task):**
- Lines: N%
- Branches: N%
- Functions: N%
- Command: [e.g., npx jest --coverage --coverageReporters=json-summary]
- Report: coverage/coverage-summary.json

**Negative testing required for:** [list ACs with validation/error/conditional logic]

---

## Tech Stack & Skills

[Copied from staging document. List each tech and the skill path to load.]

- [tech] — skill: `skills/[skill-name]/`

---

## Source Files

> Hub-updated before each dispatch. Represents current on-disk state at time of last hub update.
> Files <150 lines are included verbatim. Files ≥150 lines include only the relevant sections.
> Reviewers: use this for plan context; also run verification commands on disk for ground truth.
> QA agents: ignore this section — always read source files fresh from disk.

### [path/to/file.ext] — [CREATED | EXISTING | will be created by this task]

```lang
[file contents verbatim, or relevant sections with surrounding context]
```

---

## Library Documentation Cache

> Populated by the hub after the first implementer dispatch completes (bridges to P4).
> Empty on the initial dispatch. Hub copies key findings from the implementer's context7 Lookups.

[On re-dispatch: verbatim key excerpts from context7/Tavily results for libraries used in
this task. Implementer reads this section and skips re-querying for documented libraries.]

---

## Prior Review Feedback

> Present only on re-dispatch after a code review rejection.
> Verbatim from the reviewer's COMPLETE issues section — all Critical, Important, and Suggestion items.

[On first dispatch: omit this section entirely.]

[On re-dispatch: paste the reviewer's issues section verbatim. Include all file:line references,
code snippets, and fix recommendations exactly as provided by the reviewer.]
```

---

## Hub Instructions: Creating Context Documents

During **Phase 1b**, after creating the staging document:

1. Read each plan artifact once: `story.md`, `hld.md`, `api.md` (if present), `security.md` (if present), `testing-strategy.md` (if present), `design/` directory (if present).

2. For each task in the staging document's Task Decomposition:
   - Extract the relevant sections verbatim (not summarized) into `docs/staging/US-NNN-name.task-N.context.md`.
   - Use the plan refs with line ranges from the staging doc task entry to identify which sections to extract.
   - Include the provenance comment (`> Source: path lines X–Y`) for every verbatim section so the original can be found if needed.

3. **Task-size gate:** After extracting plan content, estimate the projected total context doc size:
   - Plan sections (already extracted) + source files (current size from task's Files list) + design references + testing requirements.
   - **Soft limit (~600 lines total):** Log a warning in the staging doc's Technical Decisions. Evaluate whether splitting the task along DU/IU boundaries produces better-scoped sub-tasks.
   - **Hard limit (~800 lines total):** The task MUST be split before proceeding. Break at the nearest DU/IU boundary, update the staging doc task list, and create separate context docs for each sub-task.
   - Log the context doc line count in each `checkpoint.sh dispatch-log` call using the `context_doc_lines` field for empirical threshold tuning.

4. Leave **Source Files** empty at Phase 1b — the hub populates it before each Phase 2 implementer dispatch.

5. Leave **Library Documentation Cache** empty at Phase 1b — populated from implementer completion summary.

6. Leave **Prior Review Feedback** absent at Phase 1b — added before re-dispatches only.

## Hub Instructions: Updating Before Each Dispatch

**Before implementer dispatch (first attempt):**
- Read each file listed in the task's `Files` section from disk.
- Write verbatim contents (files <150 lines) or relevant sections (files ≥150 lines) into the Source Files section.
- Update the `Last updated` timestamp.

**After implementer returns (before reviewer dispatch):**
- Parse the implementer's `CHANGES APPLIED` section.
- For each `CREATED` file <150 lines: read from disk, add to Source Files.
- For each `MODIFIED` file: if the before/after snippet is unambiguous, patch the Source Files section inline. If ambiguous, re-read the file from disk.
- For each `DELETED` file: remove from Source Files.
- Copy key library findings from the implementer's `context7 Lookups` section into the Library Documentation Cache.
- Update `Last updated` timestamp.

**After reviewer returns (before re-dispatch, if Changes Required):**
- Append the reviewer's COMPLETE issues section verbatim to the Prior Review Feedback section.
- Update `Last updated` timestamp.
- Do NOT re-read source files unless the reviewer flagged file contents that differ from Source Files.
